[package]
name = "delegation-registry"
version = "0.1.0"
description = "NanoClawd on-chain delegation registry"
edition = "2021"

[lib]
crate-type = ["cdylib", "lib"]
name = "delegation_registry"

[features]
no-entrypoint = []
no-idl = []
no-log-ix-name = []
cpi = ["no-entrypoint"]
default = []
idl-build = ["anchor-lang/idl-build"]

[dependencies]
anchor-lang = "0.30.1"
