{
  "id": "solana-agent",
  "name": "Solana Agent",
  "description": "Solana Agent Kit integration with 137+ actions for DeFi, NFTs, tokens, and more",
  "version": "1.0.0",
  "configSchema": {
    "type": "object",
    "properties": {
      "enabled": {
        "type": "boolean",
        "default": true,
        "description": "Enable the Solana Agent extension"
      },
      "rpcUrl": {
        "type": "string",
        "description": "Solana RPC endpoint URL (defaults to mainnet-beta)"
      },
      "network": {
        "type": "string",
        "enum": ["mainnet-beta", "devnet", "testnet"],
        "default": "mainnet-beta",
        "description": "Solana network to connect to"
      },
      "enableRemoteSigning": {
        "type": "boolean",
        "default": true,
        "description": "Enable remote wallet signing via mobile app"
      },
      "plugins": {
        "type": "array",
        "items": {
          "type": "string",
          "enum": ["token", "defi", "nft", "misc", "blinks"]
        },
        "default": ["token", "defi", "nft", "misc"],
        "description": "Solana Agent Kit plugins to load"
      },
      "privateKey": {
        "type": "string",
        "description": "Base58-encoded private key for local signing (optional, use remote signing, Privy, or SOLANA_PRIVATE_KEY env var instead)"
      },
      "privy": {
        "type": "object",
        "description": "Privy agentic wallet configuration (alternative to privateKey)",
        "additionalProperties": false,
        "properties": {
          "walletId": {
            "type": "string",
            "description": "Privy wallet ID (required for Privy mode)"
          },
          "appId": {
            "type": "string",
            "description": "Privy app ID (required for Privy mode)"
          },
          "authorizationKey": {
            "type": "string",
            "description": "Privy authorization key for server-side authentication (optional)"
          },
          "keyId": {
            "type": "string",
            "description": "Privy authorization key ID (optional, for key quorum)"
          },
          "apiUrl": {
            "type": "string",
            "description": "Privy API URL (defaults to https://api.privy.io)"
          }
        },
        "required": ["walletId", "appId"]
      },
      "openAiApiKey": {
        "type": "string",
        "description": "OpenAI API key for AI-powered features (optional)"
      }
    },
    "additionalProperties": false
  },
  "uiHints": {
    "rpcUrl": {
      "label": "RPC URL",
      "placeholder": "https://api.mainnet-beta.solana.com"
    },
    "network": {
      "label": "Network"
    },
    "enableRemoteSigning": {
      "label": "Enable Mobile Signing"
    },
    "plugins": {
      "label": "Plugins"
    },
    "privateKey": {
      "label": "Private Key",
      "placeholder": "Leave empty to use remote signing, Privy, or SOLANA_PRIVATE_KEY env var",
      "secret": true
    },
    "privy": {
      "label": "Privy Wallet",
      "description": "Configure Privy agentic wallet (alternative to private key)"
    },
    "openAiApiKey": {
      "label": "OpenAI API Key",
      "placeholder": "Optional: for AI-powered features",
      "secret": true
    }
  }
}
